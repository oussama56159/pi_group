# Host-port overrides for AeroCommand dev stack.
# These defaults avoid conflicts if you already have Postgres/EMQX running on the usual ports.
POSTGRES_HOST_PORT=25432
EMQX_MQTT_HOST_PORT=21883
EMQX_WS_HOST_PORT=21884
EMQX_DASHBOARD_HOST_PORT=28083
