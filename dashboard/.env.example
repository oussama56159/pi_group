# ─── AeroCommand Dashboard Environment Variables ───

# API Backend URL (proxied in dev, direct in production)
VITE_API_BASE_URL=/api/v1

# WebSocket endpoint
VITE_WS_BASE_URL=ws://localhost:8000/ws

# Telemetry WebSocket endpoint (mission updates)
VITE_TELEMETRY_WS_URL=ws://localhost:8000/api/v1/telemetry/ws

# MQTT broker WebSocket URL (for browser MQTT client)
VITE_MQTT_BROKER_URL=ws://localhost:8083/mqtt

# Mapbox token (if using Mapbox instead of CARTO tiles)
# VITE_MAPBOX_TOKEN=pk.xxxxx

# Enable mock mode (simulated data, no backend required)
VITE_MOCK_MODE=true

# Application title
VITE_APP_TITLE=AeroCommand

# Password recovery recipient (Dev/Admin support)
VITE_SUPPORT_EMAIL=touati.oussama@esprit.tn

# Sentry DSN for error tracking (production)
# VITE_SENTRY_DSN=https://xxxx@sentry.io/xxxx

# Analytics (production)
# VITE_GA_TRACKING_ID=G-XXXXXXX

